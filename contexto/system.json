{
  "architecture": {
    "type": "event-driven",
    "pattern": "pub/sub with request/response",
    "core_components": [
      "EventBus",
      "MQTTClient",
      "ModuleLoader",
      "UIRequestHandler",
      "ServiceExecutor",
      "HandlerLoader",
      "Logger",
      "Metrics"
    ]
  },

  "startup_sequence": [
    { "step": 1, "name": "Observability", "component": "Logger + Metrics" },
    { "step": 2, "name": "Validation", "component": "JSON Schema validator" },
    { "step": 3, "name": "MQTT", "component": "Aedes broker + client (embedded fallback)" },
    { "step": 4, "name": "Hooks", "component": "Lifecycle hooks system" },
    { "step": 5, "name": "EventBus", "component": "Event routing" },
    { "step": 5.5, "name": "UIRequestHandler", "component": "UI request/response" },
    { "step": 6, "name": "Providers", "component": "Service providers (OCR, PDF, etc)" },
    { "step": 6.5, "name": "Modules", "component": "Dynamic module loading (ordered by config.modules.enabled)" },
    { "step": 6.7, "name": "Handlers", "component": "Global handlers + project handlers" },
    { "step": 7, "name": "ServiceRegistry", "component": "Auto-allocated port" },
    { "step": 8, "name": "HTTP", "component": "Express server" }
  ],

  "module_load_order": {
    "_updated": "2026-02-16",
    "descripcion": "El orden de carga se controla con config.modules.enabled. Módulos se cargan secuencialmente en ese orden. Módulos no listados se cargan al final en orden de descubrimiento.",
    "tiers": {
      "tier_1_infra": ["credential-manager", "database-manager", "log-manager", "filesystem"],
      "tier_2_platform": ["plugin-manager", "prompt-manager", "prompt-composer", "scheduler"],
      "tier_3_core": ["project-manager", "composition-manager", "context-manager", "system-inspector", "ai-gateway", "ai-agent-framework", "agent-manager"],
      "tier_4_features": ["calling-generator", "bot-manager", "chat-ai-bridge", "chat-session", "code-executor"],
      "tier_5_domain_pizzepos": ["cuentas", "cuentas-canales", "pedidos", "cobros", "cocina", "comandero", "productos", "categorias", "ingredientes", "variaciones", "persistencia-comandero", "impresion"],
      "tier_5_domain_facturacion": ["asesoria", "fuentes"],
      "tier_6_ui": ["admin-panel", "pdf-viewer", "telegram-service", "text-editor", "menu-generator", "carta-digital", "carta-impresion"]
    },
    "razon": "credential-manager y database-manager deben cargar primero porque otros módulos (prompt-manager, project-manager) los necesitan durante su onLoad para consultas a DB y resolución de credenciales",
    "disabled": ["conversation-manager", "dashboard", "metricas", "notas", "scratch-designer", "ui-designer", "security-p2p", "staff-manager"]
  },

  "directories": {
    "core": "Core framework code (EventBus, ServiceExecutor, HandlerLoader, etc)",
    "modules": "Loadable modules (34 core [26 active + 8 disabled] + 15 pizzepos + 2 facturacion = 51 total, 43 active)",
    "handlers": "Global event handlers (JavaScript directo)",
    "frontend": "SvelteKit 2 + Svelte 5 + TypeScript app",
    "data": "Runtime data (projects, logs, scheduler, etc)",
    "data/projects/{id}": "Datos de un proyecto específico (handlers, storage, config, handler-store)",
    "data/scheduler": "Jobs del scheduler (jobs.json)",
    "services/providers": "Service providers (local.*, google.*, etc)",
    "contexto": "AI context files (29 archivos: 28 JSON + 1 MD)",
    "docs": "Human documentation (35+ archivos)",
    "prompts": "AI prompts and tutorials (15 especializados)",
    "plugins": "JSON-based plugins (github, slack, weather, http-utils)",
    "blueprints": "YAML scaffolding para generación de módulos",
    "scripts": "Utility scripts (create-module, generate-constants)",
    "network": "Multi-machine deployment scripts",
    "docker": "Docker setup y compose"
  },

  "project_structure": {
    "descripcion": "Estructura de carpetas dentro de cada proyecto",
    "base_path": "data/projects/{project-id}/",
    "carpetas": {
      "handlers": {
        "path": "handlers/*.js",
        "descripcion": "Handlers del proyecto (usan credenciales PROJECT_{id})",
        "ejemplo": "handlers/recibir-documento.js"
      },
      "storage": {
        "path": "storage/{flujo-name}/",
        "descripcion": "Almacenamiento de datos por flujo/proceso",
        "estructura": {
          "descripcion": "Cada flujo tiene su carpeta con subcarpetas según estado",
          "ejemplo_facturas": {
            "path": "storage/facturas/",
            "subcarpetas": ["recibidas", "procesadas", "enviadas", "errores"]
          }
        }
      },
      "config": {
        "path": "config/*.json",
        "descripcion": "Configuración del proyecto (accesible via context.config)"
      },
      "handler-store": {
        "path": "handler-store/{handler-name}.json",
        "descripcion": "Persistencia key-value por handler (automático)"
      }
    },
    "ejemplo_completo": {
      "project": "mi-proyecto",
      "tree": [
        "data/projects/mi-proyecto/",
        "├── handlers/",
        "│   ├── recibir-documento.js",
        "│   ├── procesar-ocr.js",
        "│   └── extraer-datos.js",
        "├── storage/",
        "│   └── facturas/",
        "│       ├── recibidas/      # Archivos originales",
        "│       ├── procesadas/     # Resultados OCR/extracción",
        "│       └── errores/        # Procesamiento fallido",
        "├── config/",
        "│   └── ocr.json           # Config de OCR",
        "└── handler-store/",
        "    └── recibir-documento.json  # Estado persistente"
      ]
    }
  },

  "conventions": {
    "naming": {
      "modules": "kebab-case (e.g., project-manager)",
      "handlers": "kebab-case.js (e.g., procesar-documento.js)",
      "events": "dot.notation (e.g., project.created)",
      "topics": "slash/notation (e.g., ui/request/project/list)",
      "files": "kebab-case.js or PascalCase.svelte",
      "services": "dot.notation (e.g., local.google-vision)"
    },
    "logging": {
      "format": "structured JSON",
      "levels": ["debug", "info", "warn", "error"],
      "required_fields": ["module", "action", "outcome"],
      "api": "logger.info('event.name', { key: value }) — primer argumento es STRING (nombre del evento), segundo es OBJETO de datos. NO usar estilo pino: logger.info({obj}, 'msg') que genera [object Object] en los logs"
    },
    "errors": {
      "always_include": ["error.message", "correlationId"],
      "never_throw": "in event handlers (catch and log)"
    }
  },

  "handlers": {
    "descripcion": "Sistema de orquestación de eventos con JavaScript directo",
    "ubicaciones": {
      "global": "handlers/*.js - Sin project_id, usa credenciales GLOBAL",
      "proyecto": "data/projects/{id}/handlers/*.js - project_id={id}, credenciales PROJECT_{id}"
    },
    "estructura_handler": {
      "name": "string - Nombre único",
      "trigger": "string - Evento que dispara el handler",
      "filter": "function(event) - Opcional, filtra eventos",
      "handle": "async function(event, {services, logger, projectId}) - Lógica"
    },
    "services_call": {
      "firma": "services.call(service, action, params)",
      "ejemplo": "await services.call('local.google-vision', 'extract', { image: path })",
      "credenciales": "Se inyecta project_id automáticamente según ubicación del handler"
    }
  },

  "services": {
    "descripcion": "Providers que ejecutan operaciones via eventos request/response",
    "patron": "{service}.{action}.request → {service}.{action}.response",
    "tipos": {
      "local": "Ejecución local (local.pdf-to-png, local.tesseract, local.sharp)",
      "cloud": "APIs externas (google.vision, anthropic.chat)"
    },
    "credenciales": {
      "resolucion": "PROJECT_{id} → GLOBAL (cascada automática)",
      "documentacion": "Ver contexto/credentials.json"
    }
  },

  "patrones_recientes": {
    "_updated": "2026-02-16",
    "page_context": {
      "descripcion": "Patrón reutilizable: página especializada + chat con contexto de lo que pasa en esa página. Dos caminos (UI manual, chat conversacional) comparten estado.",
      "motivacion": "Cada vertical (menu-generator, comandero, cocina, facturación) tendrá su página con paneles y chat contextual. El LLM sabe qué página es, qué ha hecho el usuario, y qué datos hay disponibles.",
      "componentes": {
        "page-context.ts": {
          "ubicacion": "frontend/src/lib/stores/page-context.ts",
          "tipo": "Svelte writable store genérico",
          "api": "setPageContext(), updatePageState(), updatePageStateBatch(), clearPageContext(), getPageContextSnapshot()",
          "responsabilidad": "SOLO almacenar el estado de la página actual. No sabe de chat ni de prompt."
        },
        "chat.ts": {
          "cambio": "sendMessage() captura getPageContextSnapshot() y lo envía como pageContext en el payload MQTT",
          "responsabilidad": "SOLO capturar y transportar. No sabe qué página es ni qué significa el estado."
        },
        "chat-ai-bridge": {
          "cambio": "Extrae page_context del payload, lo guarda en flowState, lo pasa a prompt.compose.request",
          "responsabilidad": "SOLO transportar. No interpreta el contenido."
        },
        "prompt-composer": {
          "cambio": "Nuevo método buildPageContextSection(). Renderiza ## Page Context con título, instrucciones, estado vivo.",
          "responsabilidad": "SOLO renderizar como sección del system prompt. No sabe de páginas específicas.",
          "truncamiento": "Valores >4000 chars se truncan. Objetos se serializan como JSON."
        },
        "+page.svelte": {
          "cambio": "onMount → setPageContext({ route, title, description, instructions, state: { projectId } }). onDestroy → clearPageContext().",
          "ubicacion": "routes/[project_id]/menu-generator/+page.svelte (project-scoped). La ruta legacy /menu-generator redirige al proyecto activo.",
          "responsabilidad": "Definir QUÉ contexto tiene esta página y QUÉ instrucciones necesita el LLM. Incluye projectId del URL param."
        },
        "paneles/componentes": {
          "cambio": "Cada panel llama updatePageState() cuando completa un paso.",
          "responsabilidad": "SOLO escribir su dato al estado. No sabe de chat ni de prompt."
        }
      },
      "flujo_completo": [
        "1. +page.svelte → setPageContext({ route, title, description, instructions, state: {} })",
        "2. Panel completa acción → updatePageState('ocrText', text) / updatePageStateBatch({...})",
        "3. Usuario envía mensaje en chat → chat.ts captura getPageContextSnapshot()",
        "4. mqttRequest('conversation', 'send', { content, pageContext }) → chat-ai-bridge",
        "5. chat-ai-bridge → prompt.compose.request con page_context",
        "6. prompt-composer.buildPageContextSection() → ## Page Context en system prompt",
        "7. LLM recibe system prompt con contexto de página → usa tools informadamente",
        "8. onDestroy → clearPageContext()"
      ],
      "primera_implementacion": "menu-generator (10 tools + 5 paneles + page context) en /[project_id]/menu-generator",
      "proximas_implementaciones": ["comandero", "cocina", "facturación"],
      "para_nueva_pagina": [
        "1. setPageContext() en +page.svelte con route, title, description, instructions",
        "2. updatePageState() en componentes cuando algo cambia",
        "3. clearPageContext() en onDestroy",
        "4. Cero cambios en backend — el patrón ya está implementado"
      ]
    },
    "tools_first_modules": {
      "descripcion": "Módulos que exponen tools para el agentic loop del chat, además de ui_handlers para el frontend",
      "patron": "module.json declara array tools[] con name, description, handler, parameters (JSON Schema)",
      "registro": "El loader registra tools en toolsRegistry → ai-gateway los carga para el LLM",
      "ejemplo": "menu-generator v4: 10 tools (generate, list, get, update_prices, add/remove product, add category, update product, search, stats)",
      "beneficio": "El usuario puede operar via chat conversacional con la misma potencia que la UI"
    },
    "project_lifecycle_caching": {
      "descripcion": "Los módulos cachean el proyecto activo via eventos en lugar de RPC repetido",
      "eventos": ["project.activated → cache", "project.deactivated → clear cache"],
      "modulos_que_cachean": ["chat-ai-bridge", "chat-session", "scheduler"],
      "beneficio": "Elimina N+1 queries a project-manager"
    },
    "direct_ui_handlers": {
      "descripcion": "Módulos declaran handlers UI en module.json, el loader los auto-wira",
      "patron_declarativo": "module.json: ui_handlers: [{ domain, action, handler }]",
      "patron_legacy": "context.uiHandler.register('domain', 'action', handler) — ya NO se usa",
      "ejemplo": "chat-ai-bridge declara { domain: 'conversation', action: 'send', handler: 'handleUISend' } en module.json",
      "nota": "conversation-manager ya NO es facade, cada módulo declara sus propios handlers"
    },
    "loader_auto_wiring": {
      "descripcion": "El ModuleLoader lee module.json y auto-conecta subscribes (con handler) y ui_handlers sin código imperativo",
      "subscribes": "events.subscribes: [{ event, handler }] → loader llama eventBus.subscribe(event, module[handler].bind(module))",
      "ui_handlers": "ui_handlers: [{ domain, action, handler }] → loader llama uiHandler.register(domain, action, module[handler].bind(module))",
      "cleanup": "El loader auto-unsubscribe y unregister en onUnload — módulos solo limpian estado propio",
      "excepciones": ["Wildcard subscriptions (e.g., agent.*.completed) — mantener imperativo", "Dynamic/runtime subscriptions — mantener imperativo"],
      "migrated_modules": "28/28 módulos core migrados (feb 2026)"
    },
    "project_scoped_operations": {
      "descripcion": "Operaciones scoped por proyecto via project_id",
      "modulos": ["chat-session (esquema SQLite por proyecto)", "scheduler (jobs con project_id)", "project-manager (composición 5 fases)"]
    },
    "project_scoped_routes": {
      "_updated": "2026-02-16",
      "descripcion": "Las páginas de verticales van bajo /[project_id]/ en vez de planas. La URL es explícita sobre qué proyecto opera.",
      "patron": "/[project_id]/pagina — ej: /peppone/menu-generator, /la-pizzeria/comandero",
      "layout_padre": "routes/[project_id]/+layout.svelte — valida proyecto, carga datos, setContext('project', store) para hijos",
      "redirect_legacy": "Rutas planas (/menu-generator) redirigen a /${activeProject.id}/menu-generator automáticamente",
      "route_matching_modulos": {
        "problema": "Manifests de módulos declaran routes: ['/menu-generator'] pero URL real es /peppone/menu-generator",
        "solucion": "routeMatches() en lazy-registry.ts stripea el primer segmento (project_id) y compara con el manifest",
        "codigo": "const withoutProject = '/' + currentRoute.split('/').slice(2).join('/'); return manifestRoutes.some(r => currentRoute.startsWith(r) || withoutProject.startsWith(r));",
        "beneficio": "Manifests NO cambian — siguen declarando /menu-generator. El matching lo resuelve."
      },
      "rutas_actuales": {
        "project_scoped": [
          "/[project_id]/comandero — pantalla de cuentas",
          "/[project_id]/comandero/[cuenta_id] — pedido de cuenta",
          "/[project_id]/menu-generator — pipeline de cartas"
        ],
        "globales": [
          "/ — home (LazyShell)",
          "/chat — chat standalone",
          "/facturas — gestión de facturas"
        ]
      },
      "para_nueva_pagina": [
        "1. Crear routes/[project_id]/mi-pagina/+page.svelte",
        "2. getContext('project') para acceder al proyecto del layout padre",
        "3. setPageContext() con route: '/mi-pagina' (sin prefijo de proyecto)",
        "4. Manifests de módulos: routes: ['/mi-pagina'] — routeMatches() lo resuelve",
        "5. Opcional: redirect en routes/mi-pagina/+page.svelte para URLs legacy"
      ]
    }
  }
}
