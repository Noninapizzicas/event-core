<script lang="ts">
  /**
   * Pagina Certificate Authority (scoped a proyecto)
   *
   * URL: /[project_id]/certificate-authority
   *
   * Page context is auto-set by lazy-registry's setCurrentRoute().
   * Workspace-specific instructions come from prompt-manager (workspace:certificate-authority).
   * Panels update live state via updatePageState().
   */
  import { LazyShell } from '$lib/components/layout';
</script>

<LazyShell />
