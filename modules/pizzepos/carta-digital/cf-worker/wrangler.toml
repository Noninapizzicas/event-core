name = "pizzicas-chat"
main = "worker.js"
compatibility_date = "2024-01-01"

# Free plan is enough: 100,000 requests/day
# usage_model = "bundled"

# Environment variables (non-secret, can also set via dashboard)
# [vars]
# ALLOWED_ORIGIN = "https://noninapizzicas.github.io"
# MAX_TOKENS = "300"

# Secrets (set via: wrangler secret put DEEPSEEK_API_KEY)
# Never put API keys in this file!
