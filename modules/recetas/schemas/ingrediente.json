{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ingrediente (Catálogo)",
  "description": "Ingrediente del catálogo del proyecto con precio de mercado y de compra",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "ID único (ing_{slug})"
    },
    "nombre": {
      "type": "string",
      "description": "Nombre del ingrediente"
    },
    "categoria": {
      "type": "string",
      "description": "Categoría (Carnes, Pescados, Verduras, Lácteos, Especias, Salsas, etc.)"
    },
    "unidad_base": {
      "type": "string",
      "description": "Unidad base para el precio (kg, l, ud)"
    },
    "precio_mercado_kg": {
      "type": "number",
      "description": "Precio de mercado por unidad base (€)"
    },
    "precio_compra_kg": {
      "type": ["number", "null"],
      "description": "Precio real de compra por unidad base (null hasta factura)"
    },
    "fuente_precio": {
      "type": "string",
      "description": "De dónde se obtuvo el precio"
    },
    "alergenos": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Lista de alérgenos"
    },
    "proveedor": {
      "type": ["string", "null"],
      "description": "Proveedor (futuro)"
    },
    "notas": {
      "type": "string"
    },
    "recetas_count": {
      "type": "number",
      "description": "En cuántas recetas aparece este ingrediente"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["id", "nombre", "unidad_base", "precio_mercado_kg"]
}
